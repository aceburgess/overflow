<%= form_for @comment do |f| %>
  <div>
    <%= f.label :content %>
    <%= f.text_field :content %>
  </div>
  <div>
    <%= f.hidden_field(:user_id, value: session[:user_id]) %>
  </div>
  <div>
    <%= f.submit %>
  </div>
<% end %>
